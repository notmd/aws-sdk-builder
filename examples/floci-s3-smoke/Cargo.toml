[package]
name = "floci-s3-smoke"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
aws-config = "1.10.1"
aws-credential-types = "1.3.0"
aws-sdk-s3 = "1.141.0"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[workspace]
