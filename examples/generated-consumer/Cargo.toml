[package]
name = "generated-consumer"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
aws-sdk-build = { path = "../../crates/aws-sdk-build" }

[build-dependencies]
aws-sdk-build = { path = "../../crates/aws-sdk-build" }
