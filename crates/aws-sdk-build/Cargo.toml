[package]
name = "aws-sdk-build"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Build-time generation of modular AWS SDK clients from Smithy models."

[dependencies]
serde_json = "1"
tempfile = "3"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
