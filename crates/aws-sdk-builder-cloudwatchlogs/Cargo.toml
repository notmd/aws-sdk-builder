[package]
name = "aws-sdk-builder-cloudwatchlogs"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Build-time AWS SDK model provider for cloudwatchlogs."
include = ["Cargo.toml", "src/**", "model.json"]

[dependencies]
aws-sdk-builder = { path = "../aws-sdk-builder" }
