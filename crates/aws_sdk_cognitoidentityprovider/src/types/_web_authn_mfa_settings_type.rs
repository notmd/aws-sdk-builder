// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A user's preference for using passkey, or WebAuthn, multi-factor authentication (MFA). Turns passkey MFA on and off for the user. Unlike other MFA settings types, this type doesn't include a <code>PreferredMfa</code> option because passkey MFA applies only when passkey is the first authentication factor.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct WebAuthnMfaSettingsType {
    /// <p>Specifies whether passkey MFA is activated for a user. When activated, the user's passkey authentication requires user verification, and passkey sign-in is available when MFA is required. The user must also have at least one other MFA method such as SMS, TOTP, or email activated to prevent account lockout.</p>
    pub enabled: bool,
}
impl WebAuthnMfaSettingsType {
    /// <p>Specifies whether passkey MFA is activated for a user. When activated, the user's passkey authentication requires user verification, and passkey sign-in is available when MFA is required. The user must also have at least one other MFA method such as SMS, TOTP, or email activated to prevent account lockout.</p>
    pub fn enabled(&self) -> bool {
        self.enabled
    }
}
impl WebAuthnMfaSettingsType {
    /// Creates a new builder-style object to manufacture [`WebAuthnMfaSettingsType`](crate::types::WebAuthnMfaSettingsType).
    pub fn builder() -> crate::types::builders::WebAuthnMfaSettingsTypeBuilder {
        crate::types::builders::WebAuthnMfaSettingsTypeBuilder::default()
    }
}

/// A builder for [`WebAuthnMfaSettingsType`](crate::types::WebAuthnMfaSettingsType).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct WebAuthnMfaSettingsTypeBuilder {
    pub(crate) enabled: ::std::option::Option<bool>,
}
impl WebAuthnMfaSettingsTypeBuilder {
    /// <p>Specifies whether passkey MFA is activated for a user. When activated, the user's passkey authentication requires user verification, and passkey sign-in is available when MFA is required. The user must also have at least one other MFA method such as SMS, TOTP, or email activated to prevent account lockout.</p>
    pub fn enabled(mut self, input: bool) -> Self {
        self.enabled = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether passkey MFA is activated for a user. When activated, the user's passkey authentication requires user verification, and passkey sign-in is available when MFA is required. The user must also have at least one other MFA method such as SMS, TOTP, or email activated to prevent account lockout.</p>
    pub fn set_enabled(mut self, input: ::std::option::Option<bool>) -> Self {
        self.enabled = input;
        self
    }
    /// <p>Specifies whether passkey MFA is activated for a user. When activated, the user's passkey authentication requires user verification, and passkey sign-in is available when MFA is required. The user must also have at least one other MFA method such as SMS, TOTP, or email activated to prevent account lockout.</p>
    pub fn get_enabled(&self) -> &::std::option::Option<bool> {
        &self.enabled
    }
    /// Consumes the builder and constructs a [`WebAuthnMfaSettingsType`](crate::types::WebAuthnMfaSettingsType).
    pub fn build(self) -> crate::types::WebAuthnMfaSettingsType {
        crate::types::WebAuthnMfaSettingsType {
            enabled: self.enabled.unwrap_or_default(),
        }
    }
}
