[package]
name = "aws-sdk-builder-dynamodb"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Build-time AWS SDK model provider for dynamodb."
include = ["Cargo.toml", "src/**", "model.json"]

[dependencies]
aws-sdk-builder = { path = "../aws-sdk-builder" }
