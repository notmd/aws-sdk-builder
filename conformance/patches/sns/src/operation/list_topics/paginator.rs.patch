--- original
+++ modified
@@ -1,8 +1,8 @@
 // Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
 /// Paginator for [`ListTopics`](crate::operation::list_topics::ListTopics)
 pub struct ListTopicsPaginator {
-    handle: std::sync::Arc<crate::client::Handle>,
-    builder: crate::operation::list_topics::builders::ListTopicsInputBuilder,
+    handle: std::sync::Arc<super::client::Handle>,
+    builder: super::operation::list_topics::builders::ListTopicsInputBuilder,
     stop_on_duplicate_token: bool,
 }

@@ -9,8 +9,8 @@
 impl ListTopicsPaginator {
     /// Create a new paginator-wrapper
     pub(crate) fn new(
-        handle: std::sync::Arc<crate::client::Handle>,
-        builder: crate::operation::list_topics::builders::ListTopicsInputBuilder,
+        handle: std::sync::Arc<super::client::Handle>,
+        builder: super::operation::list_topics::builders::ListTopicsInputBuilder,
     ) -> Self {
         Self {
             handle,
@@ -23,8 +23,8 @@
     ///
     /// This paginator automatically flattens results using `topics`. Queries to the underlying service
     /// are dispatched lazily.
-    pub fn items(self) -> crate::operation::list_topics::paginator::ListTopicsPaginatorItems {
-        crate::operation::list_topics::paginator::ListTopicsPaginatorItems(self)
+    pub fn items(self) -> super::operation::list_topics::paginator::ListTopicsPaginatorItems {
+        super::operation::list_topics::paginator::ListTopicsPaginatorItems(self)
     }

     /// Stop paginating when the service returns the same pagination token twice in a row.
@@ -47,9 +47,9 @@
         self,
     ) -> ::aws_smithy_async::future::pagination_stream::PaginationStream<
         ::std::result::Result<
-            crate::operation::list_topics::ListTopicsOutput,
+            super::operation::list_topics::ListTopicsOutput,
             ::aws_smithy_runtime_api::client::result::SdkError<
-                crate::operation::list_topics::ListTopicsError,
+                super::operation::list_topics::ListTopicsError,
                 ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
             >,
         >,
@@ -57,12 +57,12 @@
         // Move individual fields out of self for the borrow checker
         let builder = self.builder;
         let handle = self.handle;
-        let runtime_plugins = crate::operation::list_topics::ListTopics::operation_runtime_plugins(
+        let runtime_plugins = super::operation::list_topics::ListTopics::operation_runtime_plugins(
             handle.runtime_plugins.clone(),
             &handle.conf,
             ::std::option::Option::None,
         )
-        .with_operation_plugin(crate::sdk_feature_tracker::paginator::PaginatorFeatureTrackerRuntimePlugin::new());
+        .with_operation_plugin(super::sdk_feature_tracker::paginator::PaginatorFeatureTrackerRuntimePlugin::new());
         ::aws_smithy_async::future::pagination_stream::PaginationStream::new(::aws_smithy_async::future::pagination_stream::fn_stream::FnStream::new(
             move |tx| {
                 ::std::boxed::Box::pin(async move {
@@ -78,11 +78,11 @@
                         }
                     };
                     loop {
-                        let resp = crate::operation::list_topics::ListTopics::orchestrate(&runtime_plugins, input.clone()).await;
+                        let resp = super::operation::list_topics::ListTopics::orchestrate(&runtime_plugins, input.clone()).await;
                         // If the input member is None or it was an error
                         let done = match resp {
                             ::std::result::Result::Ok(ref resp) => {
-                                let new_token = crate::lens::reflens_list_topics_output_output_next_token(resp);
+                                let new_token = super::lens::reflens_list_topics_output_output_next_token(resp);
                                 // Pagination is exhausted when the next token is an empty string
                                 let is_empty = new_token.map(|token| token.is_empty()).unwrap_or(true);
                                 if !is_empty && new_token == input.next_token.as_ref() && self.stop_on_duplicate_token {
@@ -124,14 +124,14 @@
         self,
     ) -> ::aws_smithy_async::future::pagination_stream::PaginationStream<
         ::std::result::Result<
-            crate::types::Topic,
+            super::types::Topic,
             ::aws_smithy_runtime_api::client::result::SdkError<
-                crate::operation::list_topics::ListTopicsError,
+                super::operation::list_topics::ListTopicsError,
                 ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
             >,
         >,
     > {
         ::aws_smithy_async::future::pagination_stream::TryFlatMap::new(self.0.send())
-            .flat_map(|page| crate::lens::lens_list_topics_output_output_topics(page).unwrap_or_default().into_iter())
+            .flat_map(|page| super::lens::lens_list_topics_output_output_topics(page).unwrap_or_default().into_iter())
     }
 }
