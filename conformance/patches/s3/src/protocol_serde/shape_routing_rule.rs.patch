--- original
+++ modified
@@ -3,18 +3,18 @@
 pub fn de_routing_rule(
     decoder: &mut ::aws_smithy_xml::decode::ScopedDecoder,
     depth: u32,
-) -> ::std::result::Result<crate::types::RoutingRule, ::aws_smithy_xml::decode::XmlDecodeError> {
+) -> ::std::result::Result<super::types::RoutingRule, ::aws_smithy_xml::decode::XmlDecodeError> {
     if depth >= 128u32 {
         return Err(::aws_smithy_xml::decode::XmlDecodeError::custom("maximum nesting depth exceeded"));
     }
     #[allow(unused_mut)]
-    let mut builder = crate::types::RoutingRule::builder();
+    let mut builder = super::types::RoutingRule::builder();
     while let Some(mut tag) = decoder.next_tag() {
         match tag.start_el() {
             s if s.matches("Condition") /* Condition com.amazonaws.s3#RoutingRule$Condition */ =>  {
                 let var_1 =
                     Some(
-                        crate::protocol_serde::shape_condition::de_condition(&mut tag, depth + 1)
+                        super::protocol_serde::shape_condition::de_condition(&mut tag, depth + 1)
                         ?
                     )
                 ;
@@ -24,7 +24,7 @@
             s if s.matches("Redirect") /* Redirect com.amazonaws.s3#RoutingRule$Redirect */ =>  {
                 let var_2 =
                     Some(
-                        crate::protocol_serde::shape_redirect::de_redirect(&mut tag, depth + 1)
+                        super::protocol_serde::shape_redirect::de_redirect(&mut tag, depth + 1)
                         ?
                     )
                 ;
@@ -34,11 +34,11 @@
             _ => {}
         }
     }
-    Ok(crate::serde_util::routing_rule_correct_errors(builder).build())
+    Ok(super::serde_util::routing_rule_correct_errors(builder).build())
 }

 pub fn ser_routing_rule(
-    input: &crate::types::RoutingRule,
+    input: &super::types::RoutingRule,
     writer: ::aws_smithy_xml::encode::ElWriter,
 ) -> ::std::result::Result<(), ::aws_smithy_types::error::operation::SerializationError> {
     #[allow(unused_mut)]
@@ -45,11 +45,11 @@
     let mut scope = writer.finish();
     if let Some(var_3) = &input.condition {
         let inner_writer = scope.start_el("Condition");
-        crate::protocol_serde::shape_condition::ser_condition(var_3, inner_writer)?
+        super::protocol_serde::shape_condition::ser_condition(var_3, inner_writer)?
     }
     if let Some(var_4) = &input.redirect {
         let inner_writer = scope.start_el("Redirect");
-        crate::protocol_serde::shape_redirect::ser_redirect(var_4, inner_writer)?
+        super::protocol_serde::shape_redirect::ser_redirect(var_4, inner_writer)?
     }
     scope.finish();
     Ok(())
