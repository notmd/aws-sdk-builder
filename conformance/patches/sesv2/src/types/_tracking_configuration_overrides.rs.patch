--- original
+++ modified
@@ -14,7 +14,7 @@
     /// <p><code>DISABLED</code> – Amazon SES doesn't track opens for this message, even when your account-level or configuration set settings enable open tracking. Amazon SES doesn't add the tracking image to the message.</p></li>
     /// </ul>
     /// <p>If you don't specify this value, Amazon SES uses the open tracking setting that would otherwise apply to the message.</p>
-    pub open_tracking_enabled: ::std::option::Option<crate::types::FeatureStatus>,
+    pub open_tracking_enabled: ::std::option::Option<super::types::FeatureStatus>,
     /// <p>Specifies whether Amazon SES tracks when the recipient clicks a link in this message. Can be one of the following:</p>
     /// <ul>
     /// <li>
@@ -25,7 +25,7 @@
     /// <p>If you don't specify this value, Amazon SES uses the click tracking setting that would otherwise apply to the message.</p><note>
     /// <p>Enabling open or click tracking with an override doesn't create an event destination. Amazon SES records the resulting open and click events in VDM, where you can review them using VDM metrics and Message Insights. To also receive these events at a destination that you own, the configuration set that the message uses must have an event destination that publishes open and click events.</p>
     /// </note>
-    pub click_tracking_enabled: ::std::option::Option<crate::types::FeatureStatus>,
+    pub click_tracking_enabled: ::std::option::Option<super::types::FeatureStatus>,
 }
 impl TrackingConfigurationOverrides {
     /// <p>Specifies whether Amazon SES tracks when the recipient opens this message. Can be one of the following:</p>
@@ -36,7 +36,7 @@
     /// <p><code>DISABLED</code> – Amazon SES doesn't track opens for this message, even when your account-level or configuration set settings enable open tracking. Amazon SES doesn't add the tracking image to the message.</p></li>
     /// </ul>
     /// <p>If you don't specify this value, Amazon SES uses the open tracking setting that would otherwise apply to the message.</p>
-    pub fn open_tracking_enabled(&self) -> ::std::option::Option<&crate::types::FeatureStatus> {
+    pub fn open_tracking_enabled(&self) -> ::std::option::Option<&super::types::FeatureStatus> {
         self.open_tracking_enabled.as_ref()
     }
     /// <p>Specifies whether Amazon SES tracks when the recipient clicks a link in this message. Can be one of the following:</p>
@@ -49,14 +49,14 @@
     /// <p>If you don't specify this value, Amazon SES uses the click tracking setting that would otherwise apply to the message.</p><note>
     /// <p>Enabling open or click tracking with an override doesn't create an event destination. Amazon SES records the resulting open and click events in VDM, where you can review them using VDM metrics and Message Insights. To also receive these events at a destination that you own, the configuration set that the message uses must have an event destination that publishes open and click events.</p>
     /// </note>
-    pub fn click_tracking_enabled(&self) -> ::std::option::Option<&crate::types::FeatureStatus> {
+    pub fn click_tracking_enabled(&self) -> ::std::option::Option<&super::types::FeatureStatus> {
         self.click_tracking_enabled.as_ref()
     }
 }
 impl TrackingConfigurationOverrides {
     /// Creates a new builder-style object to manufacture [`TrackingConfigurationOverrides`](crate::types::TrackingConfigurationOverrides).
-    pub fn builder() -> crate::types::builders::TrackingConfigurationOverridesBuilder {
-        crate::types::builders::TrackingConfigurationOverridesBuilder::default()
+    pub fn builder() -> super::types::builders::TrackingConfigurationOverridesBuilder {
+        super::types::builders::TrackingConfigurationOverridesBuilder::default()
     }
 }

@@ -64,8 +64,8 @@
 #[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
 #[non_exhaustive]
 pub struct TrackingConfigurationOverridesBuilder {
-    pub(crate) open_tracking_enabled: ::std::option::Option<crate::types::FeatureStatus>,
-    pub(crate) click_tracking_enabled: ::std::option::Option<crate::types::FeatureStatus>,
+    pub(crate) open_tracking_enabled: ::std::option::Option<super::types::FeatureStatus>,
+    pub(crate) click_tracking_enabled: ::std::option::Option<super::types::FeatureStatus>,
 }
 impl TrackingConfigurationOverridesBuilder {
     /// <p>Specifies whether Amazon SES tracks when the recipient opens this message. Can be one of the following:</p>
@@ -76,7 +76,7 @@
     /// <p><code>DISABLED</code> – Amazon SES doesn't track opens for this message, even when your account-level or configuration set settings enable open tracking. Amazon SES doesn't add the tracking image to the message.</p></li>
     /// </ul>
     /// <p>If you don't specify this value, Amazon SES uses the open tracking setting that would otherwise apply to the message.</p>
-    pub fn open_tracking_enabled(mut self, input: crate::types::FeatureStatus) -> Self {
+    pub fn open_tracking_enabled(mut self, input: super::types::FeatureStatus) -> Self {
         self.open_tracking_enabled = ::std::option::Option::Some(input);
         self
     }
@@ -88,7 +88,7 @@
     /// <p><code>DISABLED</code> – Amazon SES doesn't track opens for this message, even when your account-level or configuration set settings enable open tracking. Amazon SES doesn't add the tracking image to the message.</p></li>
     /// </ul>
     /// <p>If you don't specify this value, Amazon SES uses the open tracking setting that would otherwise apply to the message.</p>
-    pub fn set_open_tracking_enabled(mut self, input: ::std::option::Option<crate::types::FeatureStatus>) -> Self {
+    pub fn set_open_tracking_enabled(mut self, input: ::std::option::Option<super::types::FeatureStatus>) -> Self {
         self.open_tracking_enabled = input;
         self
     }
@@ -100,7 +100,7 @@
     /// <p><code>DISABLED</code> – Amazon SES doesn't track opens for this message, even when your account-level or configuration set settings enable open tracking. Amazon SES doesn't add the tracking image to the message.</p></li>
     /// </ul>
     /// <p>If you don't specify this value, Amazon SES uses the open tracking setting that would otherwise apply to the message.</p>
-    pub fn get_open_tracking_enabled(&self) -> &::std::option::Option<crate::types::FeatureStatus> {
+    pub fn get_open_tracking_enabled(&self) -> &::std::option::Option<super::types::FeatureStatus> {
         &self.open_tracking_enabled
     }
     /// <p>Specifies whether Amazon SES tracks when the recipient clicks a link in this message. Can be one of the following:</p>
@@ -113,7 +113,7 @@
     /// <p>If you don't specify this value, Amazon SES uses the click tracking setting that would otherwise apply to the message.</p><note>
     /// <p>Enabling open or click tracking with an override doesn't create an event destination. Amazon SES records the resulting open and click events in VDM, where you can review them using VDM metrics and Message Insights. To also receive these events at a destination that you own, the configuration set that the message uses must have an event destination that publishes open and click events.</p>
     /// </note>
-    pub fn click_tracking_enabled(mut self, input: crate::types::FeatureStatus) -> Self {
+    pub fn click_tracking_enabled(mut self, input: super::types::FeatureStatus) -> Self {
         self.click_tracking_enabled = ::std::option::Option::Some(input);
         self
     }
@@ -127,7 +127,7 @@
     /// <p>If you don't specify this value, Amazon SES uses the click tracking setting that would otherwise apply to the message.</p><note>
     /// <p>Enabling open or click tracking with an override doesn't create an event destination. Amazon SES records the resulting open and click events in VDM, where you can review them using VDM metrics and Message Insights. To also receive these events at a destination that you own, the configuration set that the message uses must have an event destination that publishes open and click events.</p>
     /// </note>
-    pub fn set_click_tracking_enabled(mut self, input: ::std::option::Option<crate::types::FeatureStatus>) -> Self {
+    pub fn set_click_tracking_enabled(mut self, input: ::std::option::Option<super::types::FeatureStatus>) -> Self {
         self.click_tracking_enabled = input;
         self
     }
@@ -141,12 +141,12 @@
     /// <p>If you don't specify this value, Amazon SES uses the click tracking setting that would otherwise apply to the message.</p><note>
     /// <p>Enabling open or click tracking with an override doesn't create an event destination. Amazon SES records the resulting open and click events in VDM, where you can review them using VDM metrics and Message Insights. To also receive these events at a destination that you own, the configuration set that the message uses must have an event destination that publishes open and click events.</p>
     /// </note>
-    pub fn get_click_tracking_enabled(&self) -> &::std::option::Option<crate::types::FeatureStatus> {
+    pub fn get_click_tracking_enabled(&self) -> &::std::option::Option<super::types::FeatureStatus> {
         &self.click_tracking_enabled
     }
     /// Consumes the builder and constructs a [`TrackingConfigurationOverrides`](crate::types::TrackingConfigurationOverrides).
-    pub fn build(self) -> crate::types::TrackingConfigurationOverrides {
-        crate::types::TrackingConfigurationOverrides {
+    pub fn build(self) -> super::types::TrackingConfigurationOverrides {
+        super::types::TrackingConfigurationOverrides {
             open_tracking_enabled: self.open_tracking_enabled,
             click_tracking_enabled: self.click_tracking_enabled,
         }
