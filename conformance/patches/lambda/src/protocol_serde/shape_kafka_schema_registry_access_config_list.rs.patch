--- original
+++ modified
@@ -4,7 +4,7 @@
     _value: &'a [u8],
     depth: u32,
 ) -> ::std::result::Result<
-    Option<::std::vec::Vec<crate::types::KafkaSchemaRegistryAccessConfig>>,
+    Option<::std::vec::Vec<super::types::KafkaSchemaRegistryAccessConfig>>,
     ::aws_smithy_json::deserialize::error::DeserializeError,
 >
 where
@@ -26,7 +26,7 @@
                         break;
                     }
                     _ => {
-                        let value = crate::protocol_serde::shape_kafka_schema_registry_access_config::de_kafka_schema_registry_access_config(
+                        let value = super::protocol_serde::shape_kafka_schema_registry_access_config::de_kafka_schema_registry_access_config(
                             tokens,
                             _value,
                             depth + 1,
