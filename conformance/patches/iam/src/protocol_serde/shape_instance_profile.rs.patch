--- original
+++ modified
@@ -3,12 +3,12 @@
 pub fn de_instance_profile(
     decoder: &mut ::aws_smithy_xml::decode::ScopedDecoder,
     depth: u32,
-) -> ::std::result::Result<crate::types::InstanceProfile, ::aws_smithy_xml::decode::XmlDecodeError> {
+) -> ::std::result::Result<super::types::InstanceProfile, ::aws_smithy_xml::decode::XmlDecodeError> {
     if depth >= 128u32 {
         return Err(::aws_smithy_xml::decode::XmlDecodeError::custom("maximum nesting depth exceeded"));
     }
     #[allow(unused_mut)]
-    let mut builder = crate::types::InstanceProfile::builder();
+    let mut builder = super::types::InstanceProfile::builder();
     while let Some(mut tag) = decoder.next_tag() {
         match tag.start_el() {
             s if s.matches("Path") /* Path com.amazonaws.iam#InstanceProfile$Path */ =>  {
@@ -80,7 +80,7 @@
             s if s.matches("Roles") /* Roles com.amazonaws.iam#InstanceProfile$Roles */ =>  {
                 let var_6 =
                     Some(
-                        crate::protocol_serde::shape_role_list_type::de_role_list_type(&mut tag, depth + 1)
+                        super::protocol_serde::shape_role_list_type::de_role_list_type(&mut tag, depth + 1)
                         ?
                     )
                 ;
@@ -90,7 +90,7 @@
             s if s.matches("Tags") /* Tags com.amazonaws.iam#InstanceProfile$Tags */ =>  {
                 let var_7 =
                     Some(
-                        crate::protocol_serde::shape_tag_list_type::de_tag_list_type(&mut tag, depth + 1)
+                        super::protocol_serde::shape_tag_list_type::de_tag_list_type(&mut tag, depth + 1)
                         ?
                     )
                 ;
@@ -100,7 +100,7 @@
             _ => {}
         }
     }
-    Ok(crate::serde_util::instance_profile_correct_errors(builder)
+    Ok(super::serde_util::instance_profile_correct_errors(builder)
         .build()
         .map_err(|_| ::aws_smithy_xml::decode::XmlDecodeError::custom("missing field"))?)
 }
