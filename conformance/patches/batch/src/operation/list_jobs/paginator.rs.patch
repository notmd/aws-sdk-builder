--- original
+++ modified
@@ -1,14 +1,14 @@
 // Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
 /// Paginator for [`ListJobs`](crate::operation::list_jobs::ListJobs)
 pub struct ListJobsPaginator {
-    handle: std::sync::Arc<crate::client::Handle>,
-    builder: crate::operation::list_jobs::builders::ListJobsInputBuilder,
+    handle: std::sync::Arc<super::client::Handle>,
+    builder: super::operation::list_jobs::builders::ListJobsInputBuilder,
     stop_on_duplicate_token: bool,
 }

 impl ListJobsPaginator {
     /// Create a new paginator-wrapper
-    pub(crate) fn new(handle: std::sync::Arc<crate::client::Handle>, builder: crate::operation::list_jobs::builders::ListJobsInputBuilder) -> Self {
+    pub(crate) fn new(handle: std::sync::Arc<super::client::Handle>, builder: super::operation::list_jobs::builders::ListJobsInputBuilder) -> Self {
         Self {
             handle,
             builder,
@@ -28,8 +28,8 @@
     ///
     /// This paginator automatically flattens results using `job_summary_list`. Queries to the underlying service
     /// are dispatched lazily.
-    pub fn items(self) -> crate::operation::list_jobs::paginator::ListJobsPaginatorItems {
-        crate::operation::list_jobs::paginator::ListJobsPaginatorItems(self)
+    pub fn items(self) -> super::operation::list_jobs::paginator::ListJobsPaginatorItems {
+        super::operation::list_jobs::paginator::ListJobsPaginatorItems(self)
     }

     /// Stop paginating when the service returns the same pagination token twice in a row.
@@ -52,9 +52,9 @@
         self,
     ) -> ::aws_smithy_async::future::pagination_stream::PaginationStream<
         ::std::result::Result<
-            crate::operation::list_jobs::ListJobsOutput,
+            super::operation::list_jobs::ListJobsOutput,
             ::aws_smithy_runtime_api::client::result::SdkError<
-                crate::operation::list_jobs::ListJobsError,
+                super::operation::list_jobs::ListJobsError,
                 ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
             >,
         >,
@@ -62,12 +62,12 @@
         // Move individual fields out of self for the borrow checker
         let builder = self.builder;
         let handle = self.handle;
-        let runtime_plugins = crate::operation::list_jobs::ListJobs::operation_runtime_plugins(
+        let runtime_plugins = super::operation::list_jobs::ListJobs::operation_runtime_plugins(
             handle.runtime_plugins.clone(),
             &handle.conf,
             ::std::option::Option::None,
         )
-        .with_operation_plugin(crate::sdk_feature_tracker::paginator::PaginatorFeatureTrackerRuntimePlugin::new());
+        .with_operation_plugin(super::sdk_feature_tracker::paginator::PaginatorFeatureTrackerRuntimePlugin::new());
         ::aws_smithy_async::future::pagination_stream::PaginationStream::new(::aws_smithy_async::future::pagination_stream::fn_stream::FnStream::new(
             move |tx| {
                 ::std::boxed::Box::pin(async move {
@@ -83,11 +83,11 @@
                         }
                     };
                     loop {
-                        let resp = crate::operation::list_jobs::ListJobs::orchestrate(&runtime_plugins, input.clone()).await;
+                        let resp = super::operation::list_jobs::ListJobs::orchestrate(&runtime_plugins, input.clone()).await;
                         // If the input member is None or it was an error
                         let done = match resp {
                             ::std::result::Result::Ok(ref resp) => {
-                                let new_token = crate::lens::reflens_list_jobs_output_output_next_token(resp);
+                                let new_token = super::lens::reflens_list_jobs_output_output_next_token(resp);
                                 // Pagination is exhausted when the next token is an empty string
                                 let is_empty = new_token.map(|token| token.is_empty()).unwrap_or(true);
                                 if !is_empty && new_token == input.next_token.as_ref() && self.stop_on_duplicate_token {
@@ -129,15 +129,15 @@
         self,
     ) -> ::aws_smithy_async::future::pagination_stream::PaginationStream<
         ::std::result::Result<
-            crate::types::JobSummary,
+            super::types::JobSummary,
             ::aws_smithy_runtime_api::client::result::SdkError<
-                crate::operation::list_jobs::ListJobsError,
+                super::operation::list_jobs::ListJobsError,
                 ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
             >,
         >,
     > {
         ::aws_smithy_async::future::pagination_stream::TryFlatMap::new(self.0.send()).flat_map(|page| {
-            crate::lens::lens_list_jobs_output_output_job_summary_list(page)
+            super::lens::lens_list_jobs_output_output_job_summary_list(page)
                 .unwrap_or_default()
                 .into_iter()
         })
