--- original
+++ modified
@@ -1,14 +1,14 @@
 // Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
 /// Paginator for [`ListKeys`](crate::operation::list_keys::ListKeys)
 pub struct ListKeysPaginator {
-    handle: std::sync::Arc<crate::client::Handle>,
-    builder: crate::operation::list_keys::builders::ListKeysInputBuilder,
+    handle: std::sync::Arc<super::client::Handle>,
+    builder: super::operation::list_keys::builders::ListKeysInputBuilder,
     stop_on_duplicate_token: bool,
 }

 impl ListKeysPaginator {
     /// Create a new paginator-wrapper
-    pub(crate) fn new(handle: std::sync::Arc<crate::client::Handle>, builder: crate::operation::list_keys::builders::ListKeysInputBuilder) -> Self {
+    pub(crate) fn new(handle: std::sync::Arc<super::client::Handle>, builder: super::operation::list_keys::builders::ListKeysInputBuilder) -> Self {
         Self {
             handle,
             builder,
@@ -28,8 +28,8 @@
     ///
     /// This paginator automatically flattens results using `keys`. Queries to the underlying service
     /// are dispatched lazily.
-    pub fn items(self) -> crate::operation::list_keys::paginator::ListKeysPaginatorItems {
-        crate::operation::list_keys::paginator::ListKeysPaginatorItems(self)
+    pub fn items(self) -> super::operation::list_keys::paginator::ListKeysPaginatorItems {
+        super::operation::list_keys::paginator::ListKeysPaginatorItems(self)
     }

     /// Stop paginating when the service returns the same pagination token twice in a row.
@@ -52,9 +52,9 @@
         self,
     ) -> ::aws_smithy_async::future::pagination_stream::PaginationStream<
         ::std::result::Result<
-            crate::operation::list_keys::ListKeysOutput,
+            super::operation::list_keys::ListKeysOutput,
             ::aws_smithy_runtime_api::client::result::SdkError<
-                crate::operation::list_keys::ListKeysError,
+                super::operation::list_keys::ListKeysError,
                 ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
             >,
         >,
@@ -62,12 +62,12 @@
         // Move individual fields out of self for the borrow checker
         let builder = self.builder;
         let handle = self.handle;
-        let runtime_plugins = crate::operation::list_keys::ListKeys::operation_runtime_plugins(
+        let runtime_plugins = super::operation::list_keys::ListKeys::operation_runtime_plugins(
             handle.runtime_plugins.clone(),
             &handle.conf,
             ::std::option::Option::None,
         )
-        .with_operation_plugin(crate::sdk_feature_tracker::paginator::PaginatorFeatureTrackerRuntimePlugin::new());
+        .with_operation_plugin(super::sdk_feature_tracker::paginator::PaginatorFeatureTrackerRuntimePlugin::new());
         ::aws_smithy_async::future::pagination_stream::PaginationStream::new(::aws_smithy_async::future::pagination_stream::fn_stream::FnStream::new(
             move |tx| {
                 ::std::boxed::Box::pin(async move {
@@ -83,11 +83,11 @@
                         }
                     };
                     loop {
-                        let resp = crate::operation::list_keys::ListKeys::orchestrate(&runtime_plugins, input.clone()).await;
+                        let resp = super::operation::list_keys::ListKeys::orchestrate(&runtime_plugins, input.clone()).await;
                         // If the input member is None or it was an error
                         let done = match resp {
                             ::std::result::Result::Ok(ref resp) => {
-                                let new_token = crate::lens::reflens_list_keys_output_output_next_marker(resp);
+                                let new_token = super::lens::reflens_list_keys_output_output_next_marker(resp);
                                 // Pagination is exhausted when the next token is an empty string
                                 let is_empty = new_token.map(|token| token.is_empty()).unwrap_or(true);
                                 if !is_empty && new_token == input.marker.as_ref() && self.stop_on_duplicate_token {
@@ -129,14 +129,14 @@
         self,
     ) -> ::aws_smithy_async::future::pagination_stream::PaginationStream<
         ::std::result::Result<
-            crate::types::KeyListEntry,
+            super::types::KeyListEntry,
             ::aws_smithy_runtime_api::client::result::SdkError<
-                crate::operation::list_keys::ListKeysError,
+                super::operation::list_keys::ListKeysError,
                 ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
             >,
         >,
     > {
         ::aws_smithy_async::future::pagination_stream::TryFlatMap::new(self.0.send())
-            .flat_map(|page| crate::lens::lens_list_keys_output_output_keys(page).unwrap_or_default().into_iter())
+            .flat_map(|page| super::lens::lens_list_keys_output_output_keys(page).unwrap_or_default().into_iter())
     }
 }
